- [ ] Gather front-end HTML/CSS that needs responsiveness (already scanned some: navbar.css, home.css, index/about/product/orders/profile)
- [ ] Create/extend a single responsive CSS fix for all front pages using existing classnames (e.g., .container, .products, .card, .hero, footer, chat widget)
- [ ] Apply minimal, safe edits only to front-end files (HTML + shared CSS) without touching scripts/server logic
- [ ] Verify inline styles that break mobile (fixed widths/heights, fixed bottom/right widgets) have mobile overrides
- [ ] Run quick grep to ensure no missing @media fixes
- [ ] Test by opening main front pages on small widths (via browser devtools) and confirm layout

